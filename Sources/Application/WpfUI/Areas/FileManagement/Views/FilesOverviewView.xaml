<UserControl x:Class="Mmu.Kms.WpfUI.Areas.FileManagement.Views.FilesOverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fs="clr-namespace:Mmu.Kms.WpfUI.Areas.FileManagement.UserControls.FileSearch"
             xmlns:local="clr-namespace:Mmu.Kms.WpfUI.Areas.FileManagement.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <fs:FileSearch
            FileTags="{Binding FileTags, Mode=OneWay}"
            FileTypes="{Binding FileTypes, Mode=OneWay}"
            SearchCommand="{Binding SearchCommand, Mode=OneWay}" />

        <local:FileManagementView
            Grid.Row="1"
            DataContext="{Binding FileManagementViewModel, Mode=OneWay}" />
    </Grid>
</UserControl>

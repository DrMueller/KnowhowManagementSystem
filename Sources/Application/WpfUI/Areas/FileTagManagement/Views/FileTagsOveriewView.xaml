<UserControl x:Class="Mmu.Kms.WpfUI.Areas.FileTagManagement.Views.FileTagsOveriewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel
            Orientation="Horizontal">
            <Button Height="30" Width="80" Content="Create" />
            <Button Height="30" Width="80" Content="Update" />
            <Button Height="30" Width="80" Content="Delete" />
        </StackPanel>

        <DataGrid
            Grid.Row="1"
            ItemsSource="{Binding FileTags, Mode=OneWay}"
            VerticalScrollBarVisibility="Visible" />

    </Grid>
</UserControl>